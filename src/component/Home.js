import React, { PropTypes } from 'react';
import store from './store'

class Home extends React.Component {
  render () {
    return(
      <div>
        home
      </div>
    )
  }
}

export default Home;
